name: mlx_ffi
description: Dart FFI bindings for mlx-c generated with ffigen.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  ffi: ^2.1.2

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^5.1.1
  test: ^1.25.15
